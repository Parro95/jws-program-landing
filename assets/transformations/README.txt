Drop client transformation images here — one image per client, before &
after combined side-by-side (export from Canva is perfect).

Name them by number: 1.jpg, 2.jpg, 3.jpg ... (.png works too)
NO GAPS in the numbering — the page stops looking at the first missing
number. If you remove a photo, renumber the ones after it down.

The landing page finds them automatically — no code changes needed.
FIRST run every photo through the watermark tool — open /watermark.html
on the live site, drop the photos in, download the branded copies, and
upload THOSE here. The watermark is baked into the pixels so saved or
stolen copies still carry the JWS mark and your URL.

Upload only real clients who have given written permission to be shown.
